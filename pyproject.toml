[project]
name = "ai-news-audit"
version = "0.1.0"
description = "Auditing AI agent citation of Canadian news sources"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.40",
    "google-genai>=1.0",
    "anthropic>=0.40",
    "python-dotenv>=1.0",
]
